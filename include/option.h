#ifndef __OPTION__
#define __OPTION__

#include <stdio.h>
#include <stdlib.h>
#include <getopt.h>
#include <assert.h>

/*
typedef struct opt{
	
}Opt;
*/


/* Returns 1 if the execution has to be stopped (e.g. -help).
 * Returns 0 otherwise.
 */
/*int initOption(int argc, char * const argv[], Opt * opt);*/

#endif